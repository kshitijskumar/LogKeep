<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="logkeep.isEnabled"
            android:value="${logkeepEnabled}" />

        <!-- Optional: max log entries kept per session before oldest are dropped (default 1000) -->
        <meta-data
            android:name="logkeep.maxEntriesPerSession"
            android:value="1000" />

        <!-- Optional: max sessions kept on device before oldest session is deleted (default 5) -->
        <meta-data
            android:name="logkeep.maxSessions"
            android:value="5" />
    </application>

</manifest>