## Future ideas - good to have ones
- option to rename a session
- delete sessions
- option to create a session automatically when all sessions are deleted
  - currently session creation only happens on app start, but if user clears all the sessions, then a new session should create logging incoming logs -> similar to logcat
  - alternatively, this can be simple like logs delete from a sessions -> same as logcat
- log filtering in display
  - filter by tag text